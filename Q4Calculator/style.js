import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  numButton: {
    borderColor: 'gray',
    borderRadius: 10,
    borderWidth: 1,
    backgroundColor: 'white',
  },
});
